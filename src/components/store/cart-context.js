import React from "react";

// tạo context default
const CartContext = React.createContext({
    // items: [],
    // totalAmount: 0,
    // addItem: (item) => {},
    // removeItem: (id) => {},
});

export default CartContext;
